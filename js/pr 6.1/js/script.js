let userVariable = prompt("Введите значение переменной a");
let correctMessage = "верно";
let wrongMessage = "неверно";

if (userVariable >= 0) {
    alert(correctMessage);
} else {
    alert(wrongMessage);
}